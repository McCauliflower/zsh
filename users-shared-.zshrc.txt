# Set PATH
export PATH=$HOME/bin:$HOME/.local/bin:/opt/homebrew/bin:$PATH

# Set Oh my zsh to local instance
if [ -d "$HOME/.oh-my-zsh" ]; then
    export ZSH="$HOME/.oh-my-zsh"
    source $ZSH/oh-my-zsh.sh
else
    echo "Oh My Zsh not found in $HOME/.oh-my-zsh for user: $USER"
fi

defaults write com.apple.finder "AppleShowAllFiles" -bool "true"

# Set theme
ZSH_THEME="agnoster"

# Git prompt customization
PROMPT='👽-₵₳ɄⱠł₣ⱠØ₩ɆⱤ  %{$fg[green]%}%~%{$fg_bold[blue]%}$(git_prompt_info)%{$reset_color%} '
ZSH_THEME_GIT_PROMPT_PREFIX="("
ZSH_THEME_GIT_PROMPT_SUFFIX=")"
ZSH_THEME_GIT_PROMPT_DIRTY=" ✗"
ZSH_THEME_GIT_PROMPT_CLEAN=" ✔"

# Update settings
zstyle ':omz:update' mode auto

# Set MANPATH
export MANPATH="/usr/local/man:$MANPATH"

# Aliases
alias ..='cd ../../'
alias ...='cd ../../../'
alias ll='/bin/ls -laH'
alias n='nano'
alias reload='source /Users/Shared/.zshrc'
alias ipconfig='ip addr'
alias vscode="open -a /Applications/Visual\ Studio\ Code.app/"
# Editor
export EDITOR=nano
